# Template for creating credential secrets
# DO NOT commit actual credentials to version control!
#
# Create secrets using kubectl:
#
#   kubectl create secret generic minio-credentials \
#     --from-literal=MINIO_ACCESS_KEY_ID=minioadmin \
#     --from-literal=MINIO_SECRET_ACCESS_KEY=minioadmin
#
#   kubectl create secret generic lakefs-credentials \
#     --from-literal=LAKEFS_ACCESS_KEY_ID=${LAKEFS_ACCESS_KEY_ID} \
#     --from-literal=LAKEFS_SECRET_ACCESS_KEY=${LAKEFS_SECRET_ACCESS_KEY}
#
# Or apply this template after filling in values:

---
apiVersion: v1
kind: Secret
metadata:
  name: minio-credentials
  namespace: dfp
  labels:
    app.kubernetes.io/name: minio
    app.kubernetes.io/component: credentials
type: Opaque
stringData:
  MINIO_ACCESS_KEY_ID: "minioadmin"
  MINIO_SECRET_ACCESS_KEY: "minioadmin"

---
apiVersion: v1
kind: Secret
metadata:
  name: lakefs-credentials
  namespace: dfp
  labels:
    app.kubernetes.io/name: lakefs
    app.kubernetes.io/component: credentials
type: Opaque
stringData:
  LAKEFS_ACCESS_KEY_ID: ""
  LAKEFS_SECRET_ACCESS_KEY: ""

---
# Example: Using ExternalSecrets (if you have ESO installed)
# apiVersion: external-secrets.io/v1beta1
# kind: ExternalSecret
# metadata:
#   name: minio-credentials
#   namespace: default
# spec:
#   refreshInterval: 1h
#   secretStoreRef:
#     kind: ClusterSecretStore
#     name: vault-backend
#   target:
#     name: minio-credentials
#     creationPolicy: Owner
#   data:
#     - secretKey: MINIO_ACCESS_KEY_ID
#       remoteRef:
#         key: secret/data/minio
#         property: access_key
#     - secretKey: MINIO_SECRET_ACCESS_KEY
#       remoteRef:
#         key: secret/data/minio
#         property: secret_key
