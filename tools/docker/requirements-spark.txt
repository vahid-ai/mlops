# Requirements for Spark/Feast containers
# Used by Dockerfile.autoencoder_train and Dockerfile.spark
#
# IMPORTANT: These requirements use pandas<2 for Feast compatibility.
# This conflicts with the main pyproject.toml (pandas>=2.1), so they
# are defined separately here for Docker builds only.
#
# See docs/python-versioning.md for details.

pyspark==3.5.0
feast[spark]==0.36.0
kfp>=2.9.0
mlflow>=2.0
torch>=2.5.0,<2.6
lightning>=2.0
tensorboard>=2.0
pandas>=1.5,<2
numpy>=1.24
pyarrow>=14.0
requests>=2.31
psutil>=5.9
pyyaml>=6.0
boto3>=1.34
sqlalchemy>=1.4,<2
